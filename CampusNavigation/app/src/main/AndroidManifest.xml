<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.campusnavigation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CampusNavigation">
        <activity android:name=".EventActivity"></activity>
        <activity android:name=".WorkoutRoute" />
        <activity android:name=".WorkoutCreation" />
        <activity android:name=".PasswordRequirementsPage" />
        <activity android:name=".EmailVerifSent" />
        <activity android:name=".AccountCreation" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>